[package]
name = "pharmacy-records-desktop"
version = "0.1.0"
description = "Pharmacy Records desktop application"
authors = ["Pharmacy Records Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
